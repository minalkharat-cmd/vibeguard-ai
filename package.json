{
  "name": "vibeguard-ai",
  "version": "1.0.0",
  "description": "VibeGuard AI - On-Chain Risk Intelligence Layer for BNB Chain AI Agents",
  "private": true,
  "scripts": {
    "compile": "npx hardhat compile",
    "test": "npx hardhat test",
    "deploy:testnet": "npx hardhat run scripts/deploy.js --network bscTestnet",
    "agent": "node agent/index.js",
    "dev": "cd frontend && npm run dev"
  },
  "dependencies": {
    "ethers": "^6.13.0",
    "dotenv": "^16.4.0",
    "axios": "^1.7.0"
  },
  "devDependencies": {
    "hardhat": "^2.22.0",
    "@nomicfoundation/hardhat-toolbox": "^5.0.0",
    "@openzeppelin/contracts": "^5.1.0"
  }
}
